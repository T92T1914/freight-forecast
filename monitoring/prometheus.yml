global:
  scrape_interval: 10s

scrape_configs:
  - job_name: freight-forecast
    metrics_path: /metrics
    static_configs:
      # docker-compose service name; in k8s discovery comes from pod
      # annotations instead (see k8s/deployment.yaml)
      - targets: ["api:8000"]
